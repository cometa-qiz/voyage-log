# status.md — 実装進捗チェックリスト

> 対象: 作業の航海日誌 v1
> 仕様: `docs/requirements.md` ／ 禁止事項: `docs/constraints.md`
> 挙動リファレンス: `docs/voyage-log.html`（プロトタイプ）
>
> **運用ルール**
> - タスクは上から順に実装し、完了したら `[x]` に更新する
> - 1つのPhaseの「✅ 完了確認」がすべてパスしてから次のPhaseに進む
> - **Phase 4以降は、各Phaseの完了時に `pnpm deploy` でデプロイし、
>   公開URL（PC・スマホ実機）で動作確認することを完了条件に含める**

---

## Phase 0 ― プロジェクト初期化

**ゴール: `pnpm dev` でNext.jsの初期画面が表示され、リポジトリが整う**

### リポジトリ・雛形
- [x] GitHubリポジトリ `voyage-log` を作成する（mainへの直接コミット禁止運用）
- [x] `pnpm create next-app` でプロジェクトを作成する（TypeScript / App Router / Tailwind CSS / `src/` ディレクトリ）
- [x] `next.config.ts` に `output: 'export'` / `trailingSlash: true` / `images.unoptimized: true` を設定する
- [x] `.gitignore` に `.env*` / `out/` / `node_modules/` が含まれていることを確認する

### ドキュメント配置
- [x] `docs/requirements.md` / `docs/constraints.md` / `docs/status.md`（本ファイル）/ `docs/agent_prompt.md` / `docs/voyage-log.html` を配置する
- [x] `AGENTS.md` / `CONTRIBUTING.md` を配置する（作成済みのものをそのまま使用。内容を書き換えない）
- [x] `.env.example` を作成する（`NEXT_PUBLIC_FIREBASE_*` 6項目）

### ✅ 完了確認
- [x] `pnpm dev` で初期画面が表示される
- [x] `pnpm build` が成功し `out/` が生成される
- [x] **Phase 0での変更**がmain以外のブランチ→PR経由でmainに入っている
  （リポジトリ初期化時の最初のコミット・プッシュのみ、quickstart.md STEP 3-2の手順による例外としてmain直接でよい）

---

## Phase 1 ― Firebase設定＋認証

**ゴール: Googleサインイン・未認証リダイレクトが動く**

### Firebaseプロジェクト
- [x] 新しいFirebaseプロジェクトを作成する（Yggd-memo・ハビットトラッカーとは別）
- [x] Authentication（Google Sign-in）を有効化する
- [x] Firestoreを有効化する（本番モード）
- [x] `firestore.rules` を `docs/requirements.md` セクション6の定義通りに作成しデプロイする
- [x] `.env.local` にFirebase設定値を記入する（コミットしない）

### 実装
- [x] `src/lib/firebase.ts` を実装する（初期化・auth/dbのエクスポート）
- [ ] `src/app/login/page.tsx` を実装する（Googleサインインボタン）
- [ ] `AuthGuard.tsx`（Client Component）を実装し `layout.tsx` から呼び出す（`/login` はリダイレクト対象から除外）
- [ ] サインアウト処理を実装する（ヘッダーから呼べる）

### ✅ 完了確認
- [ ] サインイン→アプリ表示、サインアウト→`/login` へ遷移する
- [ ] 未認証で保護ページにアクセスすると `/login` へリダイレクトされる
- [ ] セキュリティルールにより自分のuid配下しか読み書きできない

---

## Phase 2 ― データ層

**ゴール: 型・計算ロジック・Firestoreフックが揃い、テストデータの読み書きができる**

### 型・定数・ロジック（`src/lib/`）
- [ ] `types.ts`: `Voyage` / `Session` / `LogEntry` / `Todo` / `Treasure` を要件定義書4.1の通り定義する（`isActive` / `schemaVersion: 5` を含む。`sessBreakMs: 0` / `pomo: null` は予約フィールド）
- [ ] `routes.ts`: `ROUTES`（3航路のpath・start/end・ゴール島座標・応援ゾーン）をプロトタイプから**数値を変えずに**移植する
- [ ] `progress.ts`: `elapsedMs()` / `progressOf()`（time / free / archivedの3分岐）/ `fmtDur()` / `fmtClock()` / `fmtDate()` を移植する
- [ ] `normalize.ts`: `normalizeVoyage()`（欠損フィールドの補完・schemaVersion管理）を実装する

### Firestoreフック（`src/hooks/`）
- [ ] `useVoyages`: `users/{uid}/voyages` の `onSnapshot` 購読（`isActive: true` のみ）、作成・更新・論理削除（`isActive: false`）を実装する
- [ ] `useTreasures`: `users/{uid}/treasures` の購読と**追記のみ**の関数を実装する
- [ ] ローカル設定（`activeId` / `view` / `muted` / `dockOpen`）をlocalStorageで管理するフックを実装する（Firestoreに保存しない）

### ✅ 完了確認
- [ ] 開発用の仮ページから航路を作成→Firestoreコンソールに反映→`onSnapshot`で画面に反映される
- [ ] 論理削除した航路が一覧から消える（ドキュメント自体は残っている）
- [ ] `progressOf()` の3分岐（時間目標・無制限・アーカイブ済み）が仕様通りの値を返す

---

## Phase 3 ― 海図の静的表示

**ゴール: ダミー航路1本で海図・島・船・航跡が正しく描画される**

### 海図（`src/components/Chart`）
- [ ] 海図SVGを移植する（海グラデ・緯度経度グリッド・水深数字・霞島／燈台礁／帆立岩・岩礁✕記号・コンパスローズ・灯台の回転光）。**座標はプロトタイプが正**
- [ ] ゴール島（浅瀬＋陸地＋桟橋＋ゴール旗）を航路の終点に描画する
- [ ] 航路の描画: 未踏区間はマゼンタ点線、通過済みは実線（`pathLength="100"` ＋ `stroke-dasharray`）
- [ ] 母港マーク・目的地マーク（パルスアニメ）・目的地名ラベルを描画する

### 船（`src/components/Ship`）
- [ ] `getTotalLength()` / `getPointAtLength()` による船の配置と接線方向への回転を実装する（**揺れは実装しない**）

### ✅ 完了確認
- [ ] ダミーデータで3航路パターンすべてを切替表示でき、船の位置・向き・航跡が進捗%と一致する
- [ ] 375px幅でレイアウトが崩れない

---

## Phase 4 ― デプロイ基盤（前倒し）

**ゴール: 公開URLでログイン〜海図表示まで動き、いつでもスマホ実機確認できる**

### デプロイ設定
- [ ] `pnpm add -D firebase-tools` で導入する
- [ ] `package.json` の `scripts` に `firebase:login` / `firebase:init` / `deploy`（`firebase deploy --only hosting`）を追加する
- [ ] `firebase init hosting` を実行し `firebase.json` の `public` が `"out"` になっていることを確認する
- [ ] `pnpm build` → `pnpm deploy` でデプロイする

### ✅ 完了確認
- [ ] 公開URLでGoogleサインイン→海図（ダミー航路）が表示される
- [ ] スマホ実機で公開URLにアクセスし表示を確認した
- [ ] **以降、各Phase完了時のデプロイ確認を運用に組み込んだ**

---

## Phase 5 ― コアループ

**ゴール: 航路の作成〜出航〜停泊〜記帳〜100%到達で完了、が公開URLで一通り動く**

### 航路管理
- [ ] 新規航路モーダルを実装する（船名／目的地名／モード選択／時間・分プルダウン／工程テキストエリア。バリデーションは要件定義書5.1）
- [ ] タブ切替を実装する（航行中は緑点滅ドット、無制限は「♾」付き進捗%、＋新しい航路）
- [ ] 航路の破棄を実装する（確認ダイアログ→論理削除）

### 出航／停泊・時間計測
- [ ] 出航（`sailing: true, sailStart: now`）／停泊（`accumMs`確定・`sessions`追記・自動記帳）を実装する
- [ ] 経過時間計算 `elapsedMs = accumMs + (sailing ? now - sailStart : 0)` で進捗を算出する（**秒加算方式に書き換えない**）
- [ ] 航行中の毎秒更新を実装する（進捗%・航行距離・航跡・船位置・航行タイマー。**クライアント計算のみでFirestoreに書き込まない**。React再レンダーを最小化する）

### 日誌
- [ ] 記帳モーダル（メモ＋記帳時点の進捗%を自動付与）を実装する
- [ ] 日誌一覧（新しい順・手動／システム記帳の区別・個別削除）を実装する

### 完了（骨格）
- [ ] 時間目標モードで航行中に100%到達→自動停泊→アーカイブ化する（演出はPhase 8。ここでは簡易メッセージでよい）

### ✅ 完了確認
- [ ] 公開URLで作成→出航→停泊→記帳の一連が動く
- [ ] 別デバイス（スマホ）で開くと航行状態・進捗が一致している
- [ ] 航行中にFirestoreへの毎秒書き込みが発生していない（Firestoreコンソールで確認）
- [ ] 100%到達で航海が完了し「完了した航海」に移る

---

## Phase 6 ― 工程管理

**ゴール: 手帳・Todoドック・無制限モードが動き、全工程完了で入港する（演出なし）**

### 工程手帳
- [ ] 手帳ドロワー（右下📖ボタン・未完了数バッジ・右スライド）を実装する
- [ ] チェック時に `doneAt` / `elapsedAtDone` を記録し、日誌に自動記帳する
- [ ] 完了工程の「⏱ ◯時間◯分 時点で完了（区間 ◯分）」表示を実装する
- [ ] チェック解除（`doneAt` / `elapsedAtDone` をnullに戻す）・工程の追加・削除を実装する

### Todoドック
- [ ] 画面左端の開閉式ドック（縦タブ「📋 工程」・金文字パネル・最大7件＋他N件・`dockOpen`保存）を実装する
- [ ] 宝物庫表示中・航路なしの場合の非表示制御を実装する

### 無制限モード
- [ ] 進捗＝工程達成率の計算を接続する
- [ ] チェック時に船が前位置→新位置へ約1秒でアニメーション前進する（解除で戻る）

### 完了（骨格）
- [ ] 全工程チェック完了（未アーカイブ）→入港処理を実装する（全速前進アニメはPhase 8。ここでは即入港でよい）
- [ ] アーカイブ済み航海では入港処理が再実行されないことを保証する

### ✅ 完了確認
- [ ] 公開URL（スマホ含む）で手帳・ドックの操作が一通り動く
- [ ] 無制限モードでチェック→船が進み、解除→戻る
- [ ] 全工程完了で入港し、時間目標モードでは目標時間が残っていても入港する

---

## Phase 7 ― 宝システム

**ゴール: 宝の獲得・図鑑・ゴール後チェックが仕様通りに動く**

### 獲得ロジック
- [ ] `grantTreasure()`（A〜Z均等抽選・`treasures`へ追記）を実装する
- [ ] 工程宝: `done数 >= 3 × (todoRewards + 1)` で排出し `todoRewards` を+1する（**チェック解除で減算しない**）
- [ ] 入港宝: 入港時に1個付与する（アーカイブ済みへの再付与なし）

### UI
- [ ] 宝獲得モーダル（金の飾り文字ポップ）を実装する
- [ ] 宝物庫タブ（26マス図鑑グリッド・所持数・図鑑◯/26種・未発見は「?」）を実装する
- [ ] 「完了した航海」に「📖 工程 n/m」ボタンを追加し、手帳がアーカイブ済み航海を開けるようにする（「入港済・チェック可」表示）

### ✅ 完了確認
- [ ] 工程3・6・9個目のチェックで宝が出る
- [ ] チェックを外して付け直しても再排出されない
- [ ] 入港で宝が1個増え、宝物庫に反映される
- [ ] アーカイブ済み航海の工程チェックで工程宝は出るが、再入港・入港宝の再付与は起きない

---

## Phase 8 ― 演出

**ゴール: 天候・時間帯・SE・応援・入港演出が揃い、プロトタイプと同じ体験になる**

### 環境演出（`useAmbience`）
- [ ] 天候: 起動時ランダム＋240秒ごとに再抽選（晴55%／曇30%／嵐15%）、CSSクラスで海図に適用する
- [ ] 天候オーバーレイ: 陽光の反射（晴×朝昼）・流れる雲・雨・稲妻フラッシュを移植する
- [ ] 時間帯: 5段階（朝→昼→夕→夜→深夜）を10分ごとに巡回、**開始はランダムoffset**。色調オーバーレイ＋星の瞬きを移植する
- [ ] 左上バッジ（時間帯＋天候）を実装する

### SE（`useSound`）
- [ ] Web Audio APIの `tone()` / `bell()` 合成を移植し、SE9種（depart / anchor / write / todoDone / todoUndo / cheer / fullspeed / treasure / arrive）を各操作に接続する
- [ ] ミュート切替（🔊／🔇・ローカル保存）を実装する
- [ ] AudioContextをユーザー操作後に生成・resumeする（iOS対策）

### 島の応援
- [ ] 応援ゾーン跨ぎ検知＋吹き出し表示（9メッセージからランダム・約4秒・SE付き）を実装する
- [ ] 同じ島は1航海1回のみ（`passed` 保存）を実装する

### 入港・全速前進
- [ ] 入港カード（紙吹雪50個・総航行時間・出航回数・戦利品の宝表示）を実装する
- [ ] 全速前進: バナー「全 速 前 進 ！」・長い汽笛＋エンジンSE・白波スプレー・現在位置→100%への3秒加速アニメ（序盤40%で行程の25%）を実装する
- [ ] 演出の発火順序（工程宝モーダル→全速前進→入港カード）を要件定義書8-5の通りにする

### ✅ 完了確認
- [ ] 公開URL（スマホ含む）で全SEが対応する操作で鳴り、ミュートで止まる
- [ ] 天候・時間帯が時間経過で切り替わり、組み合わせ（深夜×嵐など）も成立する
- [ ] 島の応援が航行中の通過時に1回だけ表示される
- [ ] 全速前進→入港カード→アーカイブの流れがプロトタイプと同等に動く
- [ ] `prefers-reduced-motion` でアニメーションが抑制される

---

## Phase 9 ― インポート／エクスポート

**ゴール: JSONでバックアップと復元ができる**

- [ ] エクスポート: 全データ（voyages・treasures等）を `voyage-log_YYYYMMDD.json` としてダウンロードする
- [ ] インポート: ファイル選択→確認ダイアログ→Firestore上の自分のデータを一括置換する（`normalizeVoyage()` を適用）
- [ ] プロトタイプ（localStorage v5）のエクスポートJSONを取り込めることを確認する（実データ移行）

### ✅ 完了確認
- [ ] エクスポート→インポートで完全に復元される
- [ ] プロトタイプで使っていたデータが本実装に移行できる

---

## Phase 10 ― 品質仕上げ

**ゴール: レスポンシブ・オフライン・セキュリティを確認し、全機能が安定して動作する**

### レスポンシブ・操作性
- [ ] スマートフォン（375px）での表示・操作を全画面で確認し崩れを修正する
- [ ] 入力欄16px（iOSズーム防止）・`touch-action: manipulation`・セーフエリア対応がプロトタイプ同等に入っていることを確認する

### オフライン・品質
- [ ] Firestoreのオフラインキャッシュを有効化し、復帰時の同期を確認する
- [ ] エラー発生時のUI表示（トースト等）を整える
- [ ] `docs/constraints.md` の全NEVERルールに違反する処理が無いか最終確認する
- [ ] Firestoreセキュリティルールの最終確認をする
- [ ] `pnpm build` → `pnpm deploy` で最終デプロイする

### ✅ 完了確認
- [ ] 他ユーザーのデータにアクセスできないことを確認した
- [ ] ネットワーク切断状態でも既存データが表示される
- [ ] 公開URLで全機能を最終確認した（PC・スマホ両方）

---

## 全体完了条件

- [ ] Phase 0〜10 のすべての完了確認がパスしている
- [ ] スマホ・PCの両方で、ログイン→航路作成→出航／停泊→工程チェック→宝獲得→入港→アーカイブ→宝物庫確認まで一通り動作する
- [ ] 別デバイスで同時に開いても、航行状態・進捗・工程・宝がリアルタイムに一致する
- [ ] プロトタイプの実データがインポートで移行済みである
